# --- Основные настройки ---
BOT_TOKEN=7636130342:AAFGi... # Токен из BotFather
IS_DEVELOPMENT=True
TIMEZONE=Europe/Moscow

# --- Настройки нейросетей ---
OPEN_ROUTER_TOKEN=sk-or-v1-... # API ключ OpenRouter
OPEN_ROUTER_MODEL=xiaomi/mimo-v2-flash:free
GROQ_API_KEY=gsk_... # API ключ Groq (если используется)

# --- База данных (Docker/Local) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=12345
POSTGRES_DB=dev_analytics_bot
POSTGRES_PORT=5432
# Внешний порт для проброса из Docker
POSTGRES_EXTERNAL_PORT=5434

# --- Настройки Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
# Для локального подключения вне Docker
REDIS_URL=redis://localhost:6379/0

# --- База данных (Разработка) ---
DEV_DATABASE_URL=postgresql+asyncpg://postgres:12345@localhost:5432/dev_analytics_bot
DEV_DB_URL_FOR_ALEMBIC=postgresql://postgres:12345@localhost:5432/dev_analytics_bot

# --- База данных (Продакшн) ---
PROD_DATABASE_URL=postgresql+asyncpg://postgres:12345@localhost:5432/prod_analytics_bot
PROD_DB_URL_FOR_ALEMBIC=postgresql://postgres:12345@localhost:5432/prod_analytics_bot

# --- Сервисные настройки (JWT и API) ---
STATIC_URL=http://localhost:8000
BACKEND_URL=http://localhost:8000
SERVICE_BOT_PUBLIC_KEY_PATH=./secrets/service_bot_public.pem
SERVICE_BOT_PRIVATE_KEY_PATH=./secrets/service_bot_private.pem
SERVICE_JWT_AUDIENCE=backend
SERVICE_JWT_TTL_SECONDS=300
