

class GetUserReactionsUseCase:
    # def __init__(self, reaction_repository: MessageReactionRepository):
    #     self._reaction_repository = reaction_repository

    # async def execute(self, username: str) -> list[MessageReaction]:
    #     return await self._reaction_repository.get_reaction_by_period(username=username)
    pass
